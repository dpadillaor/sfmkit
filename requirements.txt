# Exact versions for the container image, frozen from a clean install of the
# package's dependencies plus the [tui] extra. pyproject.toml states what sfmkit
# tolerates; this file states what the image was built and tested with.
#
# Regenerate inside a fresh container, never from a development environment:
#   docker run --rm -v "$PWD/requirements.txt:/r.txt:ro" python:3.11-slim \
#     sh -c 'pip install -q -r /r.txt "textual>=0.60" && pip freeze'
contourpy==1.3.3
cycler==0.12.1
fonttools==4.64.0
kiwisolver==1.5.1
linkify-it-py==2.2.0
markdown-it-py==4.2.0
matplotlib==3.11.1
mdit-py-plugins==0.6.1
mdurl==0.1.2
numpy==1.26.4
opencv-python-headless==4.11.0.86
packaging==26.3
pillow==12.3.0
platformdirs==4.11.8
Pygments==2.21.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
PyYAML==6.0.3
rich==15.0.0
scipy==1.17.1
six==1.17.0
textual==8.2.8
typing_extensions==4.16.0
