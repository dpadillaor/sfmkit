# PyTorch without CUDA, for the cpu image, from PyTorch's own index. Installed
# with --no-deps before requirements.txt, which brings numpy and pillow: every
# other package it needs is listed here, so nothing is resolved at build time.
#
# Frozen in a clean python:3.11-slim after
#   pip install torch==2.4.1+cpu torchvision==0.19.1+cpu --index-url <below>
--index-url https://download.pytorch.org/whl/cpu
filelock==3.32.3
fsspec==2026.7.0
Jinja2==3.1.6
MarkupSafe==3.0.3
mpmath==1.3.0
networkx==3.6.1
sympy==1.14.0
torch==2.4.1+cpu
torchvision==0.19.1+cpu
