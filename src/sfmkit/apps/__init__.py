"""User interfaces: the command line and the terminal UI.

These are the composition root -- the only layer allowed to know about all the
others. Neither contains pipeline logic; both are thin shells over the library.
"""
