"""Algorithms. Arrays in, arrays out.

Nothing here reads a file, writes a file, prints, or plots. That is what lets
the whole library be tested against synthetic scenes with exact ground truth,
in seconds, with no dataset present -- and it is enforced, not merely intended:
see the import-linter contract in pyproject.toml.
"""
